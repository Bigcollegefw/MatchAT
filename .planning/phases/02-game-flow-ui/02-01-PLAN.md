---
name: "02: Game Flow & UI Polish"
phase: 2
wave: 1
requirements_addressed:
  - FLOW-05
  - FLOW-06
  - VISU-04
files_modified:
  - Assets/Scripts/GameFlow/GameManager.cs
  - Assets/Scripts/UI/WinScreen.cs
  - Assets/Scripts/UI/LoseScreen.cs
  - Assets/Scenes/MainScene.unity
autonomous: false
---

<objective>
Implement win/lose condition display and ensure UI elements are properly positioned in the scene.
</objective>

<read_first>
- .planning/REQUIREMENTS.md
- .planning/ROADMAP.md
- Assets/Scripts/GameFlow/GameManager.cs
- Assets/Scripts/UI/TimerDisplay.cs
</read_first>

<action>
## Step 1: Create WinScreen and LoseScreen Text Components

The MainScene.unity already has WinScreen and LoseScreen GameObjects with Image components, but they need Text components for the actual message.

### 1.1 WinScreen Message
1. Select **WinScreen** in MainScene hierarchy (under Canvas)
2. Add Component → search "Text" and add **Text (Legacy)**
3. Configure the Text component:
   - Text: "You Win!"
   - Font Size: 64
   - Alignment: Center, Middle
   - Color: White
4. If WinScreen has a semi-transparent Image behind it, make sure Text is a child or placed above

### 1.2 LoseScreen Message
1. Select **LoseScreen** in MainScene hierarchy
2. Add Component → search "Text" and add **Text (Legacy)**
3. Configure:
   - Text: "Time's Up!"
   - Font Size: 64
   - Alignment: Center, Middle
   - Color: White

## Step 2: Create WinScreen.cs and LoseScreen.cs Scripts

### 2.1 WinScreen.cs
Create `Assets/Scripts/UI/WinScreen.cs`:

```csharp
using UnityEngine;
using UnityEngine.UI;

public class WinScreen : MonoBehaviour
{
    private void Start()
    {
        // Ensure win screen starts hidden
        gameObject.SetActive(false);
    }

    public void Show()
    {
        gameObject.SetActive(true);
    }

    public void Hide()
    {
        gameObject.SetActive(false);
    }
}
```

### 2.2 LoseScreen.cs
Create `Assets/Scripts/UI/LoseScreen.cs`:

```csharp
using UnityEngine;
using UnityEngine.UI;

public class LoseScreen : MonoBehaviour
{
    private void Start()
    {
        // Ensure lose screen starts hidden
        gameObject.SetActive(false);
    }

    public void Show()
    {
        gameObject.SetActive(true);
    }

    public void Hide()
    {
        gameObject.SetActive(false);
    }
}
```

## Step 3: Update GameManager to Use WinScreen/LoseScreen Scripts

The GameManager currently uses SetActive on winScreen/loseScreen GameObjects directly. Update it to use the new script components:

### 3.1 Update GameManager.cs
Modify `Assets/Scripts/GameFlow/GameManager.cs`:

```csharp
using UnityEngine;

public class GameManager : MonoBehaviour
{
    public static GameManager Instance { get; private set; }

    public bool IsGameRunning { get; private set; }
    public bool HasWon { get; private set; }

    public GameConfig gameConfig;
    public WinScreen winScreen;
    public LoseScreen loseScreen;

    void Awake()
    {
        if (Instance == null)
            Instance = this;
        else
            Destroy(gameObject);
    }

    void Start()
    {
        StartGame();
    }

    void StartGame()
    {
        IsGameRunning = true;
        HasWon = false;

        if (winScreen != null) winScreen.Hide();
        if (loseScreen != null) loseScreen.Hide();
    }

    public void CheckWinCondition()
    {
        if (!IsGameRunning) return;

        if (ScoreManager.Instance.HasReachedTarget())
        {
            EndGame(true);
        }
    }

    public void EndGame(bool won)
    {
        IsGameRunning = false;
        HasWon = won;

        if (won)
        {
            if (winScreen != null) winScreen.Show();
        }
        else
        {
            if (loseScreen != null) loseScreen.Show();
        }
    }
}
```

## Step 4: Update Scene References

In Unity Editor, update the following:

### 4.1 On GameManager object:
- Win Screen field: Drag WinScreen GameObject from Canvas
- Lose Screen field: Drag LoseScreen GameObject from Canvas

### 4.2 On WinScreen object:
- Add WinScreen.cs component (if not already added via script)

### 4.3 On LoseScreen object:
- Add LoseScreen.cs component (if not already added via script)

## Step 5: Verify UI Positioning

Ensure UI elements are clearly positioned:

- **ScoreText**: Top-left area (already at Anchor: top-left, Pos: 20, -20)
- **TimerText**: Top-right area (already at Anchor: top-right, Pos: -20, -20)
- **WinScreen**: Center of screen (should cover full canvas with centered text)
- **LoseScreen**: Center of screen (should cover full canvas with centered text)

If needed, adjust anchors and pivots to ensure text stays properly positioned on different screen sizes.

---

**Summary of Changes:**
1. Added Text components to WinScreen and LoseScreen with appropriate messages
2. Created WinScreen.cs and LoseScreen.cs for cleaner show/hide control
3. Updated GameManager to use the new WinScreen/LoseScreen scripts
4. All game flow requirements (FLOW-05, FLOW-06) will now work correctly
</action>

<acceptance_criteria>
1. **Win Screen Shows**: When score reaches 5000, WinScreen appears with "You Win!" message
2. **Lose Screen Shows**: When timer reaches 0 before score 5000, LoseScreen appears with "Time's Up!" message
3. **UI Positioned**: Score display (top-left), Timer (top-right) are clearly readable
4. **Screens Auto-Hide**: Win/Lose screens hide when new game starts
5. **Config Driven**: GameConfig references are properly set in GameManager
</acceptance_criteria>

<must_haves>
1. Win screen appears when target score (5000) is reached before time expires
2. Lose screen appears when time expires before reaching target score
3. UI elements clearly positioned and readable
</must_haves>
